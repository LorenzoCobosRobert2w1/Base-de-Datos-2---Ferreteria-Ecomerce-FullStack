# PLAN DE EJECUCIÓN ITERATIVA

El desarrollo se realizará siguiendo estrictamente estos pasos. Espera mi instrucción (ej. "Paso 1") para generar el código correspondiente a cada fase:

- **Paso 1:** Código de conexión a la base de datos MongoDB usando JavaScript.
- **Paso 2:** Funciones CRUD únicamente para la entidad `Objetos_Ferreteria`, respetando el esquema JSON definido previamente.
- **Paso 3:** Archivo HTML y CSS (estilo moderno/minimalista) para el formulario de carga de nuevos productos.
- **Paso 4:** Funciones CRUD para la entidad `Clientes`.

# REGLAS DE VALIDACIÓN Y APRENDIZAJE (CRÍTICO)

La inteligencia artificial actúa como apoyo, no como reemplazo del razonamiento analítico. Al generar cada bloque de código:
1. **Explicación:** Debes incluir comentarios claros y explicar cualquier bloque de código complejo.
2. **Testing:** Proveer instrucciones breves sobre cómo probar el código generado (ej. cómo verificar que el producto se guardó en MongoDB local o Atlas) antes de pasar al siguiente paso.
3. **Defensa de Código:** Las soluciones propuestas deben ser estándar y justificables. Prepárame para poder defender la arquitectura y las decisiones detrás de la construcción de cada CRUD.